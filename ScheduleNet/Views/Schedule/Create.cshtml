@model ScheduleNet.Models.CreateScheduledEventViewModel

@{
    ViewData["Title"] = "Create";
}

<h1>CreateScheduledEventView</h1>

<h4>CreateScheduledEventViewModel</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="CreatorEmail" class="control-label"></label>
                <input asp-for="CreatorEmail" class="form-control" />
                <span asp-validation-for="CreatorEmail" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="RequestorEmail" class="control-label"></label>
                <input asp-for="RequestorEmail" class="form-control" />
                <span asp-validation-for="RequestorEmail" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
